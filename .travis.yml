dist: trusty
language: node_js
node_js:
  - "7"
cache:
  directories:
    - "node_modules"
before_install:
  - pip install --user codecov
install:
  - npm install
script:
  - npm run lint
  - npm run coverage
after_success:
  - codecov --file coverage/lcov.info --disable search